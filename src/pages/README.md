# Pages

Full app screens are grouped by feature in this folder. Each feature folder has:

- `screens.tsx` for the current feature screen implementations.
- `index.ts` for feature-level exports.
- One file per screen as a stable screen entry point.

See `docs/screen-map.md` for the full screen-to-folder map.
